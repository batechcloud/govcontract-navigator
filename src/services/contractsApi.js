export async function fetchAllContracts(filters = {}) {
  // Stub: returns empty array so useContracts falls back to mock data.
  // Replace with real SAM.gov / USASpending API calls later.
  return [];
}
